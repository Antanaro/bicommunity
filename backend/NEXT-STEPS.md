# Следующие шаги после установки зависимостей

## ✅ Шаг 1: Установка зависимостей (если еще не сделано)

Запустите:
```
backend\install-dependencies.bat
```

Или в командной строке:
```bash
cd backend
npm install
```

## ✅ Шаг 2: Запуск миграции для таблицы токенов

Создайте таблицу для хранения токенов сброса пароля:

```bash
cd backend
npm run migrate-password-reset
```

Или запустите bat-файл (если создан):
```
backend\migrate-password-reset.bat
```

Должно появиться сообщение:
```
✅ Password reset tokens table created successfully
```

## ✅ Шаг 3: Настройка SMTP в .env

Откройте файл `backend/.env` и добавьте настройки SMTP.

### Минимальная настройка для Gmail:

```env
# Email настройки для сброса пароля
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=ваш_email@gmail.com
SMTP_PASSWORD=ваш_пароль_приложения_gmail
SMTP_FROM=ваш_email@gmail.com
FRONTEND_URL=http://localhost:3000
```

### Как получить пароль приложения Gmail:

1. Войдите в [Google Account](https://myaccount.google.com/)
2. Перейдите в **Безопасность** → **Двухэтапная аутентификация** (включите, если не включена)
3. Прокрутите вниз до **Пароли приложений**
4. Выберите **Почта** и **Другое устройство**
5. Скопируйте сгенерированный 16-значный пароль
6. Используйте его как `SMTP_PASSWORD` в `.env`

**Важно:** Не используйте обычный пароль от Gmail! Только пароль приложения.

### Альтернативные SMTP провайдеры:

См. подробную инструкцию в `PASSWORD-RESET-SETUP.md`:
- Mailgun
- SendGrid
- Яндекс.Почта
- Mail.ru

## ✅ Шаг 4: Перезапуск backend сервера

После настройки SMTP:

1. Остановите текущий backend сервер (если запущен) - нажмите `Ctrl+C`
2. Запустите заново:
   ```bash
   cd backend
   npm run dev
   ```
   
   Или используйте:
   ```
   backend\start-server.bat
   ```

3. Убедитесь, что сервер запустился без ошибок

## ✅ Шаг 5: Тестирование функциональности

### Тест 1: Запрос сброса пароля

Используйте Postman, curl или другой инструмент:

```bash
POST http://localhost:5000/api/auth/forgot-password
Content-Type: application/json

{
  "email": "существующий_email@example.com"
}
```

**Ожидаемый результат:**
- Статус 200
- Сообщение: "Если пользователь с таким email существует, на него будет отправлено письмо..."
- Письмо должно прийти на указанный email

### Тест 2: Проверка письма

1. Откройте почтовый ящик
2. Найдите письмо с темой "Сброс пароля - Форум"
3. Скопируйте токен из ссылки в письме

### Тест 3: Сброс пароля

```bash
POST http://localhost:5000/api/auth/reset-password
Content-Type: application/json

{
  "token": "токен_из_письма",
  "password": "новый_пароль123"
}
```

**Ожидаемый результат:**
- Статус 200
- Сообщение: "Пароль успешно изменен..."

### Тест 4: Вход с новым паролем

Попробуйте войти с новым паролем через обычный endpoint `/api/auth/login`

## 🔧 Решение проблем

### Ошибка: "SMTP настройки не найдены"

Убедитесь, что в `backend/.env` есть все необходимые переменные:
- `SMTP_HOST`
- `SMTP_USER`
- `SMTP_PASSWORD`

### Ошибка при отправке email

1. Проверьте правильность SMTP настроек
2. Для Gmail убедитесь, что используете пароль приложения, а не обычный пароль
3. Проверьте, что двухфакторная аутентификация включена в Google аккаунте

### Письмо не приходит

1. Проверьте папку "Спам"
2. Убедитесь, что SMTP настройки правильные
3. Проверьте логи backend сервера на наличие ошибок

## 📚 Дополнительная документация

- `PASSWORD-RESET-SETUP.md` - подробная инструкция по настройке
- `PASSWORD-RESET-QUICK-START.md` - быстрый старт
- `SETUP.md` - общая инструкция по настройке проекта

## ✨ Готово!

После выполнения всех шагов функциональность сброса пароля через email будет полностью рабочей!
