# Развертывание на виртуальную машину

## 🎯 Где запускать команду?

Команда `docker compose up -d --build` запускается **на той машине, где вы находитесь**. 

Если вы хотите развернуть на удаленную виртуальную машину, есть несколько способов:

## 📋 Вариант 1: Скопировать проект на VM и запустить там (РЕКОМЕНДУЕТСЯ)

### Шаг 1: Подключитесь к виртуальной машине

```powershell
# Через SSH (если Linux VM)
ssh username@vm-ip-address

# Или через RDP для Windows VM
# Подключитесь через Remote Desktop
```

### Шаг 2: Скопируйте проект на VM

**Способ A: Через SCP (для Linux VM)**

```powershell
# С вашего компьютера
scp -r "C:\Users\ANTANARO-PC\Desktop\Проект форума" username@vm-ip:/home/username/
```

**Способ B: Через Git (если проект в репозитории)**

```bash
# На VM
git clone your-repository-url
cd project-folder
```

**Способ C: Через общую папку/файловый менеджер**

- Скопируйте папку проекта на VM через сетевой диск или файловый менеджер

### Шаг 3: На VM установите Docker

**Для Linux (Ubuntu/Debian):**

```bash
# Обновите систему
sudo apt update

# Установите Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Установите Docker Compose
sudo apt install docker-compose-plugin

# Перезайдите в систему или выполните:
newgrp docker
```

**Для Windows Server:**

1. Скачайте Docker Desktop для Windows Server
2. Установите
3. Перезапустите сервер

### Шаг 4: На VM создайте файл .env

```bash
# На VM, в папке проекта
cd "Проект форума"
cp env.template .env
nano .env  # или vi .env
```

Заполните значения в `.env` файле.

### Шаг 5: На VM запустите Docker Compose

```bash
# На VM
cd "Проект форума"
docker compose up -d --build
```

## 📋 Вариант 2: Использовать Docker Context (для удаленного развертывания)

Это позволяет управлять удаленным Docker с вашего компьютера.

### Шаг 1: Настройте SSH доступ к VM

```powershell
# Убедитесь, что на VM установлен Docker и доступен через SSH
```

### Шаг 2: Создайте Docker Context

```powershell
# Создайте context для удаленной VM
docker context create vm-context --docker "host=ssh://username@vm-ip"

# Используйте этот context
docker context use vm-context

# Проверьте подключение
docker ps
```

### Шаг 3: Скопируйте файлы на VM

```powershell
# Используйте scp или другой способ
scp -r "C:\Users\ANTANARO-PC\Desktop\Проект форума" username@vm-ip:/path/
```

### Шаг 4: Запустите Docker Compose (файлы должны быть на VM)

```powershell
# Docker Compose будет выполняться на удаленной VM
docker compose -f /path/on/vm/docker-compose.yml up -d --build
```

## 📋 Вариант 3: Использовать CI/CD (GitHub Actions, GitLab CI и т.д.)

1. Загрузите проект в Git репозиторий
2. Настройте CI/CD pipeline
3. Pipeline автоматически развернет на VM при push

## 📋 Вариант 4: Использовать Ansible/Terraform для автоматизации

Для более сложных сценариев можно использовать инструменты автоматизации.

## 🔧 Пошаговая инструкция для типичного случая

### Предположим: у вас есть Linux VM с IP адресом

1. **Подключитесь к VM:**
   ```powershell
   ssh user@192.168.1.100
   ```

2. **Установите Docker на VM (если еще не установлен):**
   ```bash
   curl -fsSL https://get.docker.com -o get-docker.sh
   sudo sh get-docker.sh
   sudo usermod -aG docker $USER
   newgrp docker
   ```

3. **Скопируйте проект на VM:**
   ```powershell
   # С вашего Windows компьютера
   scp -r "C:\Users\ANTANARO-PC\Desktop\Проект форума" user@192.168.1.100:/home/user/
   ```

4. **На VM создайте .env файл:**
   ```bash
   cd ~/Проект\ форума
   cp env.template .env
   nano .env  # Заполните значения
   ```

5. **На VM запустите:**
   ```bash
   docker compose up -d --build
   ```

6. **Проверьте статус:**
   ```bash
   docker compose ps
   ```

## 🌐 Доступ к приложению

После развертывания приложение будет доступно по IP адресу VM:

- **Frontend:** http://vm-ip-address
- **Backend API:** http://vm-ip-address/api

## ⚙️ Настройка файрвола на VM

Убедитесь, что порты открыты:

```bash
# Для Ubuntu/Debian
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 5432/tcp  # Если нужен внешний доступ к БД
sudo ufw reload
```

## 📝 Важные моменты

1. **Docker Compose запускается там, где вы выполняете команду**
2. **Все файлы проекта должны быть на той машине, где запускается Docker Compose**
3. **Файл `.env` должен быть на VM с правильными значениями**
4. **Убедитесь, что Docker установлен на VM**

## 🔍 Проверка после развертывания

```bash
# На VM проверьте логи
docker compose logs -f

# Проверьте статус контейнеров
docker compose ps

# Проверьте, что приложение доступно
curl http://localhost/api/health
```

## 💡 Рекомендация

**Самый простой способ:**
1. Скопируйте весь проект на VM
2. Подключитесь к VM по SSH
3. Создайте `.env` файл
4. Запустите `docker compose up -d --build` на VM

Это самый надежный и понятный способ развертывания.
