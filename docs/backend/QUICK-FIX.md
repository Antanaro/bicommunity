# Быстрое исправление ошибки 500 и ECONNREFUSED

## Проблема
- Ошибка 500 при регистрации
- ECONNREFUSED - frontend не может подключиться к backend

## Решение за 3 шага

### Шаг 1: Исправьте порт в .env

**Проблема:** В файле `.env` указан `PORT=5432` (это порт PostgreSQL!)

**Решение:**
1. Откройте файл `backend/.env` в блокноте
2. Найдите строку: `PORT=5432`
3. Измените на: `PORT=5000`
4. Сохраните файл

Или запустите: `fix-port.bat` или `check-and-fix.bat`

### Шаг 2: Убедитесь, что backend запущен

**В отдельном терминале:**
```bash
cd backend
npm run dev
```

**Должно появиться:**
```
🚀 Server running on http://localhost:5000
📝 Environment: development
🔐 JWT Secret: ✅ Set
🗄️  Database: forum_db on 127.0.0.1:5432
✅ Database connected
```

### Шаг 3: Проверьте подключение

Откройте в браузере: http://localhost:5000/api/health

Должно вернуть: `{"status":"ok","message":"Database connected"}`

## Если все еще не работает

### Проверьте консоль backend

Если в консоли backend есть ошибки, скопируйте их и проверьте:

1. **Ошибка с базой данных** - проверьте подключение:
   ```bash
   npm run check-db
   ```

2. **Ошибка с JWT_SECRET** - проверьте файл .env:
   ```bash
   check-config.bat
   ```

3. **Ошибка компиляции TypeScript** - все должно быть исправлено

## Важно помнить

- `PORT=5000` - порт для Express сервера (backend)
- `DB_PORT=5432` - порт PostgreSQL (не меняйте!)
- Backend должен быть запущен **отдельно** или через `npm run dev` в корне проекта
