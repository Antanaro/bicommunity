# Решение ошибки "unable to get image" - Docker API 500

## ❌ Проблема

Ошибка:
```
unable to get image 'postgres:15-alpine': request returned 500 Internal Server Error
for API route and version http://%2F%2F.%2Fpipe%2FdockerDesktopLinuxEngine/v1.51/...
```

## ✅ Решения

### Решение 1: Перезапустите Docker Desktop (САМОЕ ПРОСТОЕ)

1. **Закройте Docker Desktop полностью:**
   - Правый клик на иконке Docker в трее (системный трей)
   - Выберите "Quit Docker Desktop"
   - Дождитесь полного закрытия

2. **Запустите Docker Desktop снова:**
   - Откройте Docker Desktop из меню Пуск
   - Дождитесь, пока иконка в трее станет зеленой (Docker запущен)

3. **Попробуйте снова:**
   ```powershell
   docker compose up -d --build
   ```

### Решение 2: Проверьте статус Docker

```powershell
# Проверьте, что Docker работает
docker ps

# Проверьте версию Docker
docker --version

# Проверьте информацию о системе
docker info
```

Если команды не работают, Docker Desktop не запущен или есть проблемы.

### Решение 3: Обновите Docker Desktop

1. Откройте Docker Desktop
2. Перейдите в Settings (Настройки)
3. Проверьте наличие обновлений
4. Если есть обновления - установите их
5. Перезапустите Docker Desktop

### Решение 4: Используйте другой образ PostgreSQL

Если проблема только с конкретным образом, попробуйте другой тег:

В `docker-compose.yml` измените:
```yaml
image: postgres:15-alpine
```

На:
```yaml
image: postgres:15
# или
image: postgres:latest
```

### Решение 5: Очистите Docker

```powershell
# Остановите все контейнеры
docker stop $(docker ps -aq)

# Очистите неиспользуемые образы
docker system prune -a

# Перезапустите Docker Desktop
# Затем попробуйте снова
docker compose up -d --build
```

### Решение 6: Проверьте интернет-соединение

Убедитесь, что у вас есть доступ в интернет для загрузки образов:

```powershell
# Попробуйте загрузить образ вручную
docker pull postgres:15-alpine
```

Если это не работает, проблема может быть в:
- Брандмауэре
- Прокси-сервере
- VPN
- Корпоративной сети

### Решение 7: Сброс Docker Desktop

Если ничего не помогает:

1. Откройте Docker Desktop
2. Перейдите в Settings → Troubleshoot
3. Нажмите "Reset to factory defaults"
4. **⚠️ ВНИМАНИЕ:** Это удалит все контейнеры, образы и volumes!
5. Перезапустите Docker Desktop

### Решение 8: Используйте WSL 2 Backend

Если используете Windows:

1. Откройте Docker Desktop Settings
2. Перейдите в General
3. Убедитесь, что включено "Use the WSL 2 based engine"
4. Примените изменения
5. Перезапустите Docker Desktop

## 🔍 Диагностика

### Проверьте логи Docker Desktop

1. Откройте Docker Desktop
2. Перейдите в Settings → Troubleshoot
3. Просмотрите логи для диагностики

### Проверьте, что Docker Engine запущен

```powershell
# Должно вернуть информацию о Docker
docker info
```

Если команда не работает, Docker Engine не запущен.

## 📋 Пошаговая инструкция (рекомендуется)

1. **Закройте Docker Desktop полностью** (Quit из трея)
2. **Подождите 10-15 секунд**
3. **Запустите Docker Desktop снова**
4. **Дождитесь, пока иконка станет зеленой**
5. **Проверьте:**
   ```powershell
   docker ps
   ```
6. **Попробуйте загрузить образ:**
   ```powershell
   docker pull postgres:15-alpine
   ```
7. **Если образ загрузился, запустите:**
   ```powershell
   docker compose up -d --build
   ```

## ⚠️ Если ничего не помогает

1. Переустановите Docker Desktop
2. Убедитесь, что у вас последняя версия Windows
3. Проверьте, что включена виртуализация в BIOS
4. Обратитесь в поддержку Docker
