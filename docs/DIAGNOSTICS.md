# Диагностика проблемы ECONNREFUSED

## Проблема
Frontend не может подключиться к backend (ошибка ECONNREFUSED).

## Причины и решения

### 1. Backend не запущен

**Симптомы:**
- Ошибка `ECONNREFUSED` в консоли frontend
- Нет сообщения "🚀 Server running on http://localhost:5000" в консоли backend

**Решение:**
1. Откройте отдельный терминал
2. Перейдите в папку backend:
   ```bash
   cd backend
   ```
3. Запустите сервер:
   ```bash
   npm run dev
   ```
4. Или используйте bat-файл:
   ```bash
   start-server.bat
   ```

### 2. Backend упал из-за ошибки

**Симптомы:**
- В консоли backend видна ошибка
- Сервер не запускается

**Решение:**
1. Проверьте консоль backend на наличие ошибок
2. Убедитесь, что все файлы сохранены
3. Проверьте, что файл `.env` существует и правильно настроен
4. Попробуйте перезапустить сервер

### 3. Порт 5000 занят другим процессом

**Симптомы:**
- Ошибка "Port 5000 is already in use"

**Решение:**
1. Найдите процесс, использующий порт 5000:
   ```bash
   netstat -ano | findstr :5000
   ```
2. Завершите процесс или измените порт в `.env`:
   ```
   PORT=5001
   ```
3. Обновите `vite.config.ts` в frontend:
   ```typescript
   target: 'http://localhost:5001',
   ```

### 4. Проблема с переменными окружения

**Симптомы:**
- Сервер запускается, но падает при первом запросе
- Ошибки в консоли backend

**Решение:**
1. Проверьте файл `.env`:
   ```bash
   cd backend
   check-config.bat
   ```
2. Убедитесь, что все переменные установлены:
   - PORT
   - DB_HOST
   - DB_PORT
   - DB_NAME
   - DB_USER
   - DB_PASSWORD
   - JWT_SECRET

## Проверка работоспособности

### Шаг 1: Проверьте, запущен ли backend

Откройте в браузере: http://localhost:5000/api/health

Должно вернуть:
```json
{"status":"ok","message":"Database connected"}
```

Если не работает - backend не запущен.

### Шаг 2: Проверьте консоль backend

В терминале, где запущен backend, должно быть:
```
🚀 Server running on http://localhost:5000
📝 Environment: development
🔐 JWT Secret: ✅ Set
🗄️  Database: forum_db on 127.0.0.1:5432
✅ Database connected
```

### Шаг 3: Проверьте консоль frontend

В консоли браузера (F12) не должно быть ошибок `ECONNREFUSED`.

## Быстрое решение

1. **Остановите все процессы** (Ctrl+C в терминалах)

2. **Запустите backend отдельно:**
   ```bash
   cd backend
   npm run dev
   ```

3. **В другом терминале запустите frontend:**
   ```bash
   cd frontend
   npm run dev
   ```

4. **Или используйте корневой скрипт:**
   ```bash
   npm run dev
   ```

## Если ничего не помогает

1. Проверьте, что Node.js установлен: `node --version`
2. Проверьте, что все зависимости установлены: `npm install` в обеих папках
3. Проверьте файл `.env` в папке backend
4. Проверьте логи в консоли backend
