# 🔍 Диагностика ошибки ECONNREFUSED

Если вы видите ошибку `ECONNREFUSED` при попытке подключиться к `/api/categories`, это означает, что **backend сервер не запущен** или **не может запуститься**.

## Быстрая диагностика:

### 1. Проверьте, запущен ли backend сервер

Откройте новый терминал и выполните:
```bash
cd backend
npm run dev
```

**Что должно произойти:**
- Сервер должен запуститься и показать: `🚀 Server running on http://localhost:5000`
- Если вы видите ошибки - читайте дальше

### 2. Проверьте файл .env

Убедитесь, что в папке `backend` есть файл `.env`:
```bash
cd backend
dir .env
```

Если файла нет:
```bash
copy env.txt .env
```

### 3. Проверьте настройки в .env

Откройте `backend/.env` и убедитесь:
- `PORT=5000` (НЕ 5432!)
- `DB_PASSWORD=ваш_пароль_postgresql`
- Все остальные настройки заполнены

### 4. Проверьте подключение к базе данных

```bash
cd backend
call check-db.bat
```

Если ошибка - проверьте:
- Запущен ли PostgreSQL (services.msc → PostgreSQL)
- Правильный ли пароль в .env
- Создана ли база данных `forum_db`

### 5. Создайте базу данных (если нужно)

```bash
cd backend
call create-database.bat
call migrate.bat
```

### 6. Запустите сервер с проверками

```bash
cd backend
call check-and-start.bat
```

Этот скрипт автоматически проверит все настройки и запустит сервер.

## Частые проблемы:

### ❌ Проблема: "Database connection error"
**Решение:** 
1. Проверьте, запущен ли PostgreSQL
2. Проверьте пароль в .env
3. Убедитесь, что база данных `forum_db` создана

### ❌ Проблема: "Port 5000 already in use"
**Решение:**
1. Найдите процесс, использующий порт 5000
2. Остановите его или измените PORT в .env

### ❌ Проблема: "Cannot find module"
**Решение:**
```bash
cd backend
npm install
```

## После исправления:

1. Запустите backend:
   ```bash
   cd backend
   npm run dev
   ```

2. В другом терминале запустите frontend:
   ```bash
   cd frontend
   npm run dev
   ```

3. Откройте браузер: http://localhost:3000

4. Проверьте, что ошибка исчезла
