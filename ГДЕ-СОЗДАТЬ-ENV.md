# Где создать файл .env?

## 📍 Расположение файла

Файл `.env` должен быть создан **в корне проекта**, в той же папке, где находится `docker-compose.yml`.

## 📂 Структура проекта:

```
Проект форума/
├── .env                    ← ЗДЕСЬ создайте файл .env
├── docker-compose.yml      ← Рядом с этим файлом
├── nginx.conf
├── backend/
│   └── ...
├── frontend/
│   └── ...
└── nginx/
    └── ...
```

## ✅ Правильный путь:

```
c:\Users\ANTANARO-PC\Desktop\Проект форума\.env
```

## 📝 Как создать файл:

### Вариант 1: Через командную строку (PowerShell)

```powershell
cd "c:\Users\ANTANARO-PC\Desktop\Проект форума"
New-Item -Path .env -ItemType File
notepad .env
```

### Вариант 2: Через проводник Windows

1. Откройте папку: `c:\Users\ANTANARO-PC\Desktop\Проект форума`
2. Создайте новый текстовый файл
3. Переименуйте его в `.env` (включая точку в начале!)
4. Откройте и заполните содержимое

### Вариант 3: Через редактор кода (VS Code, Cursor и т.д.)

1. Откройте папку проекта в редакторе
2. Создайте новый файл с именем `.env`
3. Заполните содержимое

## ⚠️ Важно:

- Файл должен называться именно `.env` (с точкой в начале)
- Файл должен быть в корне проекта (рядом с `docker-compose.yml`)
- НЕ создавайте файл в папках `backend/` или `frontend/` - только в корне!

## 📋 Содержимое файла:

Я уже создал файл `.env` с примером содержимого. Откройте его и заполните значения:

- `DB_PASSWORD` - пароль для PostgreSQL
- `JWT_SECRET` - секретный ключ (сгенерируйте: `openssl rand -hex 32`)

После заполнения можно запускать:
```bash
docker-compose up -d --build
```
