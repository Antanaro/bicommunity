# Решение проблем с регистрацией

## Частые ошибки и решения

### 1. "Не удалось подключиться к серверу"

**Причина:** Backend не запущен или недоступен.

**Решение:**
1. Убедитесь, что backend запущен:
   ```bash
   cd backend
   npm run dev
   ```
2. Проверьте, что сервер работает на http://localhost:5000
3. Откройте в браузере http://localhost:5000/api/health - должно вернуть `{"status":"ok"}`

### 2. "Пользователь уже существует"

**Причина:** Пользователь с таким именем или email уже зарегистрирован.

**Решение:**
- Используйте другое имя пользователя или email
- Или войдите в существующий аккаунт

### 3. Ошибки валидации

**Возможные ошибки:**
- "Username must be 3-50 characters" - имя пользователя должно быть от 3 до 50 символов
- "Invalid email" - неверный формат email
- "Password must be at least 6 characters" - пароль должен быть минимум 6 символов

**Решение:**
- Проверьте, что все поля заполнены правильно
- Имя пользователя: 3-50 символов
- Email: правильный формат (например, user@example.com)
- Пароль: минимум 6 символов

### 4. "Ошибка сервера" или "Server error"

**Причина:** Проблема на стороне сервера (база данных, код и т.д.)

**Решение:**
1. Проверьте консоль backend - там будет детальная информация об ошибке
2. Убедитесь, что база данных запущена и доступна
3. Проверьте файл `.env` в папке backend
4. Проверьте логи в консоли браузера (F12 → Console)

### 5. CORS ошибки

**Причина:** Проблемы с настройками CORS на backend.

**Решение:**
- Убедитесь, что backend использует cors middleware
- Проверьте, что frontend обращается к правильному URL

## Диагностика

### Проверка backend

1. Откройте терминал в папке backend
2. Запустите: `npm run dev`
3. Должно появиться: `🚀 Server running on http://localhost:5000`

### Проверка подключения к API

Откройте в браузере:
- http://localhost:5000/api/health - должно вернуть `{"status":"ok","message":"Database connected"}`

### Проверка в консоли браузера

1. Откройте DevTools (F12)
2. Перейдите на вкладку Console
3. Попробуйте зарегистрироваться
4. Посмотрите на ошибки в консоли

### Проверка Network

1. Откройте DevTools (F12)
2. Перейдите на вкладку Network
3. Попробуйте зарегистрироваться
4. Найдите запрос к `/api/auth/register`
5. Посмотрите на статус и ответ сервера

## Тестирование через curl

Проверьте, работает ли API напрямую:

```bash
curl -X POST http://localhost:5000/api/auth/register \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"testuser\",\"email\":\"test@example.com\",\"password\":\"password123\"}"
```

Если это работает, проблема в frontend. Если нет - проблема в backend.

## Логи для отладки

Включите детальное логирование:

**Backend:** Логи уже включены в консоли при `npm run dev`

**Frontend:** Откройте консоль браузера (F12) для просмотра ошибок

## Контакты для помощи

Если проблема не решена:
1. Проверьте консоль backend - там детальная информация
2. Проверьте консоль браузера (F12)
3. Проверьте Network tab в DevTools
4. Убедитесь, что все зависимости установлены
