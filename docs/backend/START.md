# 🎉 База данных настроена!

База данных и все таблицы успешно созданы.

## Следующие шаги

### 1. Запустите приложение

В корневой папке проекта:
```bash
npm run dev
```

Это запустит одновременно:
- Backend на http://localhost:5000
- Frontend на http://localhost:3000

Или запустите отдельно:

**Backend:**
```bash
cd backend
npm run dev
```

**Frontend (в другом терминале):**
```bash
cd frontend
npm run dev
```

### 2. Откройте браузер

Перейдите на http://localhost:3000

### 3. Зарегистрируйте первого пользователя

1. Нажмите "Регистрация"
2. Заполните форму:
   - Имя пользователя
   - Email
   - Пароль (минимум 6 символов)
3. Нажмите "Зарегистрироваться"

### 4. Создайте первого администратора

Чтобы создавать категории, нужно сделать пользователя администратором:

**Через SQL:**
```sql
-- Подключитесь к базе данных
psql -U postgres -d forum_db

-- Сделайте пользователя администратором (замените email на ваш)
UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';
```

**Или через pgAdmin:**
1. Откройте pgAdmin
2. Подключитесь к серверу
3. Откройте базу данных `forum_db`
4. Выполните SQL запрос выше

### 5. Создайте первую категорию

После того как вы стали администратором, категории можно создавать через API:

**Через Postman/Insomnia:**
```
POST http://localhost:5000/api/categories
Headers:
  Authorization: Bearer ваш_jwt_токен
  Content-Type: application/json
Body:
{
  "name": "Общие обсуждения",
  "description": "Общие темы для общения"
}
```

**Или через curl:**
```bash
curl -X POST http://localhost:5000/api/categories \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer ваш_jwt_токен" \
  -d "{\"name\": \"Общие обсуждения\", \"description\": \"Общие темы для общения\"}"
```

JWT токен можно получить после регистрации/входа - он сохраняется в localStorage браузера.

## Готово! 🚀

Теперь вы можете:
- Регистрировать пользователей
- Создавать категории (если вы администратор)
- Создавать темы обсуждений
- Комментировать в темах
- Ставить лайки сообщениям

## Полезные команды

- `npm run dev` - запуск всего приложения
- `cd backend && npm run dev` - только backend
- `cd frontend && npm run dev` - только frontend
- `cd backend && npm run check-db` - проверка подключения к БД

## Структура API

- `GET /api/categories` - список категорий
- `GET /api/topics?category_id=1` - темы категории
- `GET /api/topics/:id` - тема с сообщениями
- `POST /api/topics` - создать тему (требуется авторизация)
- `POST /api/posts` - создать сообщение (требуется авторизация)
- `POST /api/posts/:id/like` - лайкнуть сообщение

Полное описание API в README.md
