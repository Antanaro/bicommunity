# 🚀 Быстрый старт

## ✅ База данных настроена!

Все таблицы созданы успешно. Теперь можно запускать приложение.

## Запуск приложения

### Вариант 1: Всё сразу (рекомендуется)

В корневой папке проекта:
```bash
npm run dev
```

Откройте браузер: http://localhost:3000

### Вариант 2: Отдельно

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm run dev
```

## Первые шаги

1. **Откройте** http://localhost:3000
2. **Зарегистрируйтесь** - создайте аккаунт
3. **Станьте администратором** (см. ниже)
4. **Создайте категорию** через API или добавьте через SQL
5. **Создайте тему** на форуме

## Сделать пользователя администратором

```sql
-- Через psql
psql -U postgres -d forum_db
UPDATE users SET role = 'admin' WHERE email = 'ваш_email';
```

Или через pgAdmin - выполните тот же SQL запрос.

## Создать категорию через SQL (быстрый способ)

```sql
INSERT INTO categories (name, description) 
VALUES ('Общие обсуждения', 'Общие темы для общения');
```

## Готово!

Теперь можно использовать форум! 🎉
